<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mubeen Rehman | B2B Lead Generation Expert</title>
    <style>
        /* Modern Reset and Base Styles */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
        }
        body {
            background-color: #0f172a; /* Premium Dark Theme Background */
            color: #f8fafc;
            line-height: 1.6;
        }

        /* Header Section */
        header {
            background: linear-gradient(135deg, #1e293b 0%, #0f172a 100%);
            padding: 60px 20px;
            text-align: center;
            border-bottom: 2px solid #38bdf8;
        }
        header h1 {
            font-size: 3rem;
            color: #38bdf8; /* Vibrant Cyan */
            margin-bottom: 10px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        header p {
            font-size: 1.3rem;
            color: #94a3b8;
            max-width: 600px;
            margin: 0 auto;
        }

        /* Main Container */
        .container {
            max-width: 1100px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        /* Poster Banner Section */
        .poster-section {
            background: #1e293b;
            padding: 20px;
            border-radius: 16px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            text-align: center;
            margin-bottom: 50px;
            border: 1px solid #334155;
        }
        .poster-section h2 {
            font-size: 1.8rem;
            color: #f1f5f9;
            margin-bottom: 20px;
        }
        .poster-wrapper {
            position: relative;
            display: inline-block;
            width: 100%;
            max-width: 700px; /* Adjusted for optimal presentation */
        }
        .poster-wrapper img {
            width: 100%;
            height: auto;
            border-radius: 8px;
            display: block;
            box-shadow: 0 5px 15px rgba(0,0,0,0.3);
        }

        /* Services Section */
        .services-title {
            text-align: center;
            font-size: 2.2rem;
            color: #38bdf8;
            margin-bottom: 30px;
            position: relative;
        }
        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
            margin-bottom: 50px;
        }
        .service-card {
            background: #1e293b;
            padding: 30px;
            border-radius: 12px;
            border: 1px solid #334155;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .service-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(56, 189, 248, 0.2);
            border-color: #38bdf8;
        }
        .service-card h3 {
            color: #38bdf8;
            font-size: 1.4rem;
            margin-bottom:
